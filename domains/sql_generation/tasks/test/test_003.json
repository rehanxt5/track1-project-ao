{
  "id": "test_003",
  "question": "For customers in 'Miami', how many completed orders has each one placed? Return customer name and order count, only for customers with at least one completed order, ordered by count descending then name ascending.",
  "gold": {
    "sql": "SELECT c.name AS name, COUNT(*) AS order_count FROM orders o JOIN customers c ON c.id = o.customer_id WHERE o.status = 'completed' AND c.city = 'Miami' GROUP BY c.id ORDER BY order_count DESC, c.name ASC",
    "columns": [
      "name",
      "order_count"
    ],
    "expected_rows": [
      [
        "Umar Smith",
        5
      ],
      [
        "Sam Smith",
        1
      ]
    ],
    "order_matters": true
  }
}
