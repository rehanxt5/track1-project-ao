{
  "id": "dev_008",
  "question": "How many orders from customers in 'Seattle' have status 'pending' or 'cancelled'? Return a single column named order_count.",
  "gold": {
    "sql": "SELECT COUNT(*) AS order_count FROM orders o JOIN customers c ON c.id = o.customer_id WHERE c.city = 'Seattle' AND o.status IN ('pending', 'cancelled')",
    "columns": [
      "order_count"
    ],
    "expected_rows": [
      [
        2
      ]
    ],
    "order_matters": false
  }
}
