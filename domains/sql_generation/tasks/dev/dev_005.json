{
  "id": "dev_005",
  "question": "How many orders from customers in 'Boston' have status 'pending' or 'cancelled'? Return a single column named order_count.",
  "gold": {
    "sql": "SELECT COUNT(*) AS order_count FROM orders o JOIN customers c ON c.id = o.customer_id WHERE c.city = 'Boston' AND o.status IN ('pending', 'cancelled')",
    "columns": [
      "order_count"
    ],
    "expected_rows": [
      [
        4
      ]
    ],
    "order_matters": false
  }
}
